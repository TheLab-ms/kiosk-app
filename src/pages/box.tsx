import React from 'react';

const Box = () => {
  const boxStyle = {
    width: '1440px',
    height: '960px',
    backgroundColor: '#8A947F',
  };

  return <div style={boxStyle}></div>;
};

export default Box;
